# MongoDB Connection URI
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?appName=<app>"

# Public site URL used for canonical URLs, metadata, sitemap, and robots.txt
NEXT_PUBLIC_SITE_URL="https://aiprompts.mommentx.space"

# Session Secret (Generate a strong random 32-byte hex string)
SESSION_SECRET="your-32-byte-secret-key-here"
